<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Work Order</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 40px;
        }

        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            border-bottom: 2px solid #000;
            padding-bottom: 10px;
            font-family: Arial, sans-serif;
        }

        .company-info {
            flex: 1;
        }

        .logo-name {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .logo-name img {
            width: 80px;
        }

        .company-details {
            margin-top: 10px;
            font-size: 16px;
        }

        .info-row {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            align-items: center;
            margin-top: 4px;
        }

        .underlined-bold {
            text-decoration: underline;
            font-weight: bold;
        }

        .link {
            font-weight: bold;
            text-decoration: underline;
        }

        .work-order-title {
            flex: 0 0 auto;
            margin-top: 10px;
            text-align: center;
        }

        .work-order-details {
            flex: 0 0 auto;
            text-align: right;
        }

        .order-no {
            font-size: 25px;
            font-weight: 900;
        }

        .order-date-value {
            font-size: 15px;
            font-weight: 600;
        }

        .bottom-info {
            display: flex;
            justify-content: center;
            margin-top: -20px;
            font-size: 16px;
            font-weight: bold;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        th,
        td {
            border: 1px solid #000;
            padding: 6px;
            text-align: center;
            font-size: 14px;
        }

        .urgent-row {
            background-color: #e0e0e0;
            font-weight: bold;
            text-align: left;
            padding: 8px;
        }

        .title-row {
            font-weight: bold;
            text-align: center;
            font-size: 18px;
            text-decoration: underline;
            padding: 10px;
        }

        .no-border {
            border: none;
        }

        .summary-title {
            font-weight: bold;
            text-decoration: underline;
            margin-top: 20px;
            margin-bottom: 5px;
        }

        .summary-table th,
        .summary-table td {
            border: 1px solid #aaa;
            font-size: 14px;
        }

        .summary-table {
            border-collapse: collapse;
            width: 100%;
        }

        @media print {
            body {
                margin: 0;
                padding: 10mm;
                font-size: 12px;
                box-sizing: border-box;
            }

            table,
            .summary-table {
                width: 100%;
                border-collapse: collapse;
                table-layout: auto;
            }

            th,
            td {
                padding: 6px;
                text-align: center;
                font-size: 12px;
                white-space: nowrap;
            }

            .no-print {
                display: none !important;
            }

            img {
                max-width: 100%;
                height: auto;
            }
        }

        @media screen and (max-width: 600px) {
            body {
                margin: 10px;
                font-size: 14px;
            }

            .top-bar {
                flex-direction: column;
                align-items: flex-start;
            }

            .company-info {
                flex-direction: column;
                align-items: flex-start;
            }

            .work-order {
                text-align: left;
                margin-top: 10px;
            }

            .company-info img {
                margin-bottom: 10px;
            }

            table,
            .summary-table {
                font-size: 12px;
            }

            .title-row {
                font-size: 16px;
                padding: 5px;
            }

            th,
            td {
                padding: 4px;
            }
        }

        @media print {
            thead {
                display: table-header-group;
            }

            tfoot {
                display: table-footer-group;
            }

            tr {
                page-break-inside: avoid;
            }
        }
    </style>
</head>

<body>
    <!-- Center Section: Work Order Title -->
    <div class="work-order-title">
        <h2><strong>WORK ORDER</strong></h2>
    </div>
    <div class="top-bar">
        <!-- Left Section: Logo + Info -->
        <div class="company-info">
            <div class="logo-name">
                <img src="{{logoUrl}}" alt="Logo" />
                <h2><strong>Turakhia Overseas Private Limited</strong></h2>
            </div>

            <div class="company-details">
                <div class="info-row">
                    <strong>Customer Name:</strong>
                    <span class="underlined-bold">{{order.owner_name}}</span>
                </div>

                <div class="info-row">
                    <strong>Branding Type:</strong>
                    <span>{{order.branding_type}}</span>
                </div>

                <div class="info-row">
                    <strong>Client by:</strong>
                    <span>{{order.order_received_from}}</span>

                    <strong>Plywood Sub Type:</strong>
                    <span> {{base_sub_category}}</span>
                </div>

            </div>
        </div>

        <!-- Right Section: Work Order No & Date -->
        <div class="work-order-details">
            <div>
                <strong>Work Order No:</strong>
                <span class="order-no">{{order.order_no}}</span>
            </div>
            <div class="order-date">
                <strong>Order Date:</strong>
                <span class="order-date-value">{{order.orderDateFormatted}}</span>
            </div>
        </div>
    </div>

    <div style="
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin: 10px 0;
      ">
        <div>
            <h2 style="font-weight: bold; text-decoration: underline; margin: 0">
                URGENT
            </h2>
        </div>
        <div class="title-row" style="
          font-weight: bold;
          font-size: 18px;
          text-decoration: underline;
          margin-top: -85px;
          margin-right: 0px;
        ">
            {{order.order_category}}
            {{base_type}}
        </div>
    </div>
    <div style="display: flex">
        <table>
            <thead>
                <tr style="text-decoration: underline">
                    <th>Series</th>
                    <th style="max-width: 300px;">Sales Itemname</th>
                    <th>Photo Number</th>
                    <th>LogX</th>
                    <th>Character</th>
                    <th>Length</th>
                    <th>Width</th>
                    <th>Sheet</th>
                    <th>SqMtr</th>
                    <th>Remarks</th>
                </tr>
            </thead>

            <tbody>
                {{#each items}}
                <tr>
                    <td>{{series_name}}</td>
                    <td style="max-width: 300px; white-space: normal; word-wrap: break-word; word-break: break-word;">
                        {{sales_item_name}}</td>
                    <td>{{photo_number}}</td>
                    <td>{{group_no}}</td>
                    <td>{{character}}</td>
                    <td>{{length}}</td>
                    <td>{{width}}</td>
                    <td>{{no_of_sheets}}</td>
                    <td>{{sqm}}</td>
                    <td>{{remark}}</td>
                </tr>
                {{/each}}
                <tr>
                    <td colspan="7" style="text-align: right; font-weight: bold">
                        Total :
                    </td>
                    <td>{{totalSheets}}</td>
                    <td>{{totalSqMtr}}</td>
                    <td colspan="6"></td>
                </tr>
            </tbody>
        </table>
    </div>

    <div class="summary-title">Order Summary</div>
    <table class="summary-table" cellspacing="0" cellpadding="6" style="border-collapse: collapse; width: 100%;">
        <thead>
            <tr>
                <td colspan="3"></td>
                <th>Sheet</th>
                <th>Sq.Mtr.</th>
            </tr>
        </thead>

        <tbody>
            {{#each groupedSeries}}
            {{#each items}}
            <tr>
                <td style="width: 100px;">
                    {{!-- {{#if @first}}{{../series_name}}{{/if}} --}}
                    {{../series_name}}
                </td>
                <td colspan="2"
                    style="max-width: 300px; white-space: normal; word-wrap: break-word; word-break: break-word;">
                    {{sales_item_name}}</td>
                <td style="text-align: right;"><strong>{{no_of_sheets}}</strong></td>
                <td style="text-align: right;"><strong>{{sqm}}</strong></td>
            </tr>
            {{/each}}
            {{/each}}

            <!-- Summary Totals -->
            <tr>
                <td></td>
                <td colspan="2" style="text-align: left; font-weight: bold;">Total</td>
                <td style="text-align: right; font-weight: bold;">{{totalSheets}}</td>
                <td style="text-align: right; font-weight: bold;">{{totalSqMtr}}</td>
            </tr>
            <tr>
                <td colspan="3" style="text-align: left; font-weight: bold;">Total</td>
                <td style="text-align: right; font-weight: bold;">{{totalSheets}}</td>
                <td style="text-align: right; font-weight: bold;">{{totalSqMtr}}</td>
            </tr>
        </tbody>
    </table>

    <div class="approved" style="display: flex; gap: 20px; margin-top: 200px;">
        <div>APPROVED BY: __________________</div>
        <div>RECEIVED BY: __________________</div>
    </div>
</body>

</html>